# ===========================================
# MCP 智能化问答应用 - 环境变量配置模板
# ===========================================
# 复制此文件为 .env 并填入你的实际配置值

# ===========================================
# 云端AI服务配置 (必需)
# ===========================================
# API易 API密钥 (注册地址: https://api.apiyi.com)
OPENAI_API_KEY=your-apiyi-api-key-here

# API易 服务地址
OPENAI_BASE_URL=https://api.apiyi.com/v1

# 默认使用的云端模型
OPENAI_MODEL=deepseek-r1

# ===========================================
# 本地AI模型配置 (可选)
# ===========================================
# 本地GGUF模型文件路径 (如果不使用本地模型可以留空)
# 示例: /Users/username/models/qwen2-1.5b-instruct-q4_k_m.gguf
GGUF_MODEL_PATH=

# ===========================================
# 数据库配置 (使用Docker时保持默认即可)
# ===========================================
# MySQL医疗数据库连接字符串
HOSPITAL_DB_URL=mysql+pymysql://root:password@127.0.0.1:3306/hospital_db

# PostgreSQL仓储数据库连接字符串
WAREHOUSE_DB_URL=postgresql://postgres:password@127.0.0.1:5432/warehouse_db

# ===========================================
# 应用配置 (可选)
# ===========================================
# 应用端口
PORT=8000

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 是否启用调试模式
DEBUG=false

# ===========================================
# 安全配置 (可选)
# ===========================================
# 密钥 (用于JWT等加密)
SECRET_KEY=your-secret-key-here

# 访问令牌过期时间 (秒)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===========================================
# 注意事项
# ===========================================
# 1. 不要将 .env 文件提交到Git仓库
# 2. 确保 OPENAI_API_KEY 是有效的
# 3. 本地模型路径必须是绝对路径
# 4. 数据库密码在生产环境中应该更复杂
